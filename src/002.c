#include <stdio.h>




int main(void)
{
	int N = 0;  //  input data number
	int row = 0, col = 0;  // row: n, col: m

	scanf("%d", &N);

	int data_count = 0;
	for (data_count = 0; data_count < N; data_count++)
	{
		scanf("%d %d", &row, &col);

		


	
	}

	return 0;
}
